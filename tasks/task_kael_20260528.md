# Task: Kael VPS nginx 安全加固
## 分配给：kael (47.253.179.11)
## 优先级：P1
## 路径：/root/tical-code/

你是 root，所有操作不需要 sudo。
有 git push 权限（指向 eitelite-codebase-audit）。

---

## Task 1：加 nginx 安全头

```bash
# 找到 nginx 配置
ls /etc/nginx/sites-enabled/
# 编辑配置，listen 443 ssl; 后加：
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-Frame-Options "DENY" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
```

**验证**：
```bash
curl -s -D - https://47.253.179.11/ | grep -iE 'x-content|x-frame|csp|hsts'
nginx -t && nginx -s reload
```

---

## Task 2：加 favicon + robots.txt

---

## 完成条件
- [ ] nginx 安全头已加并验证
- [ ] git commit + git push
